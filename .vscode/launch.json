{
	"version": "0.2.0",
	"configurations": [
		
		// Use this to debug the extension's code.
		// Launch another VSCode window with the duktape project you want 
		// to debug and have it define the following property
		// int a debug configuration of your launch.json file:
		// 		"debugServer" : 4711,
        {
            
            //"preLaunchTask"     : "build",
			"name"        : "DukDebug server",
			"type"        : "node",
			"request"     : "launch",
			"runtimeArgs" : [ "--nolazy" ],
			"program"     : "${workspaceRoot}/out/main.js",
			"stopOnEntry" : false,
			"args"        : [ "--server=4711" ],
			"sourceMaps"  : true,
			"outFiles"    : [ "${workspaceRoot}/out/*.js"],
			"cwd"         : "${workspaceRoot}"
		},

		// Use this to debug the gulp build pipeline
		{
			"name"        : "Debug Gulp Pipeline",
			"type"        : "node",
			"stopOnEntry" : false,
			"program"     : "${workspaceRoot}/node_modules/gulp/bin/gulp.js",
			//"program"     : "${workspaceRoot}/makeExtension.js",
			"args"        : [ "build" ],
			"cwd"         : "${workspaceRoot}",
			"sourceMaps"  : false
		},
		
		// Launch the extension in a new window.
		// Kinda useless for debugging.
		{
			"name"              : "Launch Extension",
			"type"              : "extensionHost",
			"request"           : "launch",
			"runtimeExecutable" : "${execPath}",
			"stopOnEntry"       : false,
			"sourceMaps"        : true,
			"outDir"            : "${workspaceRoot}/out",
            
            //"preLaunchTask"     : "build",
            
			"args": [
				"--extensionDevelopmentPath=${workspaceRoot}",
				"--server=4711"
			]
		}
        
	]
}